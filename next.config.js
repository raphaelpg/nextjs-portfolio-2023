/** @type {import('next').NextConfig} */
const nextConfig = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['Monoton', 'sans-serif'],
        body: ['Monoton', 'sans-serif'],
      },
    },
  },
}

module.exports = nextConfig